[project]
name = "oneclick-agent"
version = "0.1.0"
description = "One-Click Report Analysis Agent — Strands on AgentCore Runtime"
requires-python = ">=3.10"
dependencies = [
    "strands-agents>=1.0",
    "boto3>=1.34.0",
    "bedrock-agentcore>=0.1.0",
]
